

## Security baseline

The deployed application enforces explicit-search-only departmental access, server-side department-scoped projections, Admin-only global routes, signed short-lived lookup tickets, and workbook imports with audited duplicate-safe persistence.
